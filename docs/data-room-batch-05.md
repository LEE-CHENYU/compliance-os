# Data Room Batch 05

Source folder: `/Users/lichenyu/Desktop/Important Docs `

## Batch 05 focus

This batch is reserved for remaining identity, travel, and archive-heavy records that still matter for retrieval and verification:

- additional `I-20` history and travel evidence
- passport, visa, and personal archive overflow
- remaining tax and root-level identity records not already covered by earlier batches

## Proposed manifest

The exact 10-file working set has not been materialized yet. The initial source scope is `i20`, `Personal Info Archive`, `Tax`, and selected root-level files.

## Current batch blockers

1. Batch 05 manifest is not yet materialized from the source folder into a concrete 10-file test set.
2. Remaining archive-heavy families need a tighter priority order so the first pass targets the documents that materially improve review and retrieval.
3. Cross-document review rules for travel and identity consistency are not defined yet.

## Next queue

1. Materialize the first 10-file manifest from `i20`, `Personal Info Archive`, `Tax`, and root-level records.
2. Rank remaining families by review value, not just by raw file count.
3. Add the next retrieval and consistency checks once the manifest is fixed.
